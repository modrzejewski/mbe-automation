[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mbe-automation"
version = "0.1.0"
dependencies = [
  "torch>=2.8.0",
  "numpy",
  "scipy",
  "ase",
  "dscribe",
  "mace-torch",
  "matplotlib",
  "pymatgen",
  "pyscf",
  "scikit-learn",
  "spglib",
  "seekpath",
  "phonopy",
  "tqdm",
  "joblib",
  "h5py",
  "pandas",
  "tables",
  "ipi",
  "doped",
]
